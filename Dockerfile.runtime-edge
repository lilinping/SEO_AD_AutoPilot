FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY apps/runtime_edge/ apps/runtime_edge/

EXPOSE 8080

CMD ["python", "-m", "uvicorn", "apps.runtime_edge.main:create_app", "--factory", "--host", "0.0.0.0", "--port", "8080"]
