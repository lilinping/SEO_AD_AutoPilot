# Skill Registry

This folder stores the clean-room SEO-AD AutoPilot skill catalog.

The backend loads `registry.json` to expose skills, threshold metadata, approval flags, rollback support, and observability hints.
